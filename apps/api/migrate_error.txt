Environment variables loaded from .env
Prisma schema loaded from prisma\schema.prisma
Datasource "db": PostgreSQL database "postgres", schema "public" at "aws-1-ap-south-1.pooler.supabase.com:5432"

Drift detected: Your database schema is not in sync with your migration history.

The following is a summary of the differences between the expected database schema given your migrations files, and the actual schema of the database.

It should be understood as the set of changes to get from the expected schema to the actual schema.

[+] Added enums
  - Priority
  - WorkOrderStatus

[+] Added tables
  - DowntimeLog
  - MaintenanceRecord
  - Shift
  - SparePart
  - WorkOrder

[*] Changed the `BatchStatus` enum
  [+] Added variant `SCHEDULED`

[*] Changed the `DowntimeLog` table
  [+] Added foreign key on columns (machineId)

[*] Changed the `MaintenanceRecord` table
  [+] Added foreign key on columns (machineId)

[*] Changed the `ProductionBatch` table
  [+] Added column `operatorId`
  [+] Added column `shiftId`
  [+] Added column `workOrderId`
  [+] Added foreign key on columns (operatorId)
  [+] Added foreign key on columns (shiftId)
  [+] Added foreign key on columns (workOrderId)

[*] Changed the `SparePart` table
  [+] Added unique index on columns (partNumber)

[*] Changed the `WorkOrder` table
  [+] Added unique index on columns (workOrderNumber)

We need to reset the "public" schema at "aws-1-ap-south-1.pooler.supabase.com:5432"

You may use prisma migrate reset to drop the development database.
All data will be lost.
